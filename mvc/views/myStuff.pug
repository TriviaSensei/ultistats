extends _base

block styles 
    link(rel='stylesheet' href='/css/user.css')

block content 
    section.navbar-container
        .container-fluid
            .container-fill
                include components/navbar
    section.profile-body
        h3 My Stuff
        ul.nav.nav-tabs.nav-fill.mb-3#myTab(role="tablist")
            li.nav-item(role="presentation")
                button.nav-link.active#team-tab(data-bs-toggle="tab" data-bs-target="#team-div" type="button" role="tab" aria-controls="team-div" aria-selected="true") Teams
            li.nav-item(role="presentation")
                button.nav-link#tournament-tab(data-bs-toggle="tab" data-bs-target="#tournament-div" type="button" role="tab" aria-controls="tournament-div" aria-selected="false") Tournaments
            li.nav-item(role="presentation")
                button.nav-link#game-tab(data-bs-toggle="tab" data-bs-target="#game-div" type="button" role="tab" aria-controls="game-div" aria-selected="false") Games
        .tab-content#myTabContent
            .tab-pane.fade.show.active#team-div(role="tabpanel" aria-labelledby="team-tab" tabindex="0")
                .tab-inner.flex-col.ai-c
                    include user/teams
            .tab-pane.fade#tournament-div(role="tabpanel" aria-labelledby="tournament-tab" tabindex="0")
                include user/tournaments
            .tab-pane.fade#game-div(role="tabpanel" aria-labelledby="game-tab" tabindex="0")
                include user/games

block scripts 
    script(src="/js/teams.js" type="module" defer)